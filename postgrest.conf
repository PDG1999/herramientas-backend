# PostgREST Configuration für SAMEBI Backend

# Database Connection
db-uri = "postgres://postgres:secure_password_123@localhost:5432/herramientas"
db-schemas = "api"
db-anon-role = "web_anon"

# JWT Settings
jwt-secret = "your-jwt-secret-key-here-min-32-chars"

# Server Settings
server-host = "0.0.0.0"
server-port = 3000

# Connection Pool
db-pool = 10
db-pool-acquisition-timeout = 10

# CORS Settings
server-cors-allowed-origins = "*"

# OpenAPI
openapi-server-proxy-uri = "https://api.samebi.net"

# Logging
log-level = "info"

# Health Check Endpoint
admin-server-port = 3001
